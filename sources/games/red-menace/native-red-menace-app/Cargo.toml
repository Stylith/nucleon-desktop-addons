[package]
name = "robcos-native-red-menace-app"
version = "0.4.4"
edition = "2021"
license = "GPL-3.0-only"
publish = false

[lib]
path = "src/lib.rs"

[[bin]]
name = "robcos-red-menace"
path = "src/main.rs"

[dependencies]
egui = "0.29"
image = { version = "0.25", default-features = false, features = ["png"] }
robcos-hosted-addon-contract = { path = "../../../sdk/hosted-addon-contract" }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
anyhow = "1"
eframe = { version = "0.29", default-features = false, features = ["default_fonts", "glow", "x11", "wayland"] }
