[package]
name = "robcos-native-nuke-codes-app"
version = "0.4.4"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
